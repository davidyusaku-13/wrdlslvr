{
  "name": "wrdlslvr",
  "module": "src/server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "setup": "bun run scripts/setup.ts",
    "dev": "bun run --hot src/server.ts",
    "test": "bun run src/test_solver.ts",
    "wrangler-dev": "bunx wrangler dev",
    "deploy": "bunx wrangler deploy"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
